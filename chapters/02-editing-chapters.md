# Editing Chapters

The document is organized as individual Markdown chapter files so each chapter
can be edited independently.

Add new chapters in the `chapters` directory with numeric prefixes so their
order stays clear, then include them in the Pandoc command you use to build the
finished document.
