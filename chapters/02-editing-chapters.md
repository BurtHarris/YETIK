# Editing Chapters

The document is organized as individual Markdown chapter files so each chapter
can be edited independently.

Add new chapters in the `chapters` directory and list them in `pandoc.yaml` in
the order they should appear in the finished document.
